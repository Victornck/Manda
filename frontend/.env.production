# Build de produção. O front e o back ficam no MESMO domínio (o Express serve o
# build), então a API é relativa: "/api". As demais variáveis (VITE_GOOGLE_CLIENT_ID
# etc.) são herdadas do arquivo .env normal.
VITE_API_URL=/api
